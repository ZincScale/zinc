main() {
    ch Chan<String> = Chan(2)

    go {
        ch.send("hello from goroutine")
        ch.send("second message")
    }

    msg1 := ch.receive()
    msg2 := ch.receive()
    print(msg1)
    print(msg2)
}
