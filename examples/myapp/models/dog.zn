package "myapp/models"

Dog : Animal {
    pub List<String> tricks

    new(String name) {
        super(name, "Woof")
    }

    pub learnTrick(String trick) {
        this.tricks.add(trick)
    }

    pub Int trickCount() {
        return this.tricks.size()
    }
}
