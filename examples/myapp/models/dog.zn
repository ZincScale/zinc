package "myapp/models"

Dog : Animal {
    var tricks List<String>

    new(name String) {
        super(name, "Woof")
    }

    pub learnTrick(trick String) {
        this.tricks.add(trick)
    }

    pub trickCount() Int {
        return this.tricks.size()
    }
}
