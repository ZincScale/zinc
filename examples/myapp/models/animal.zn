package "myapp/models"

Animal {
    name String
    sound String

    new(name String, sound String) {
        this.name = name
        this.sound = sound
    }

    pub speak() String {
        return "{this.name} says {this.sound}!"
    }

    pub getName() String {
        return this.name
    }
}
