// Greeter — lives in actors package, extends Actor

import zinc.Actor

class Greeter : Actor {
    var String prefix = "hello"

    init(String prefix) {
        this.prefix = prefix
    }

    pub fn greet(String name): String {
        return "{prefix} {name}"
    }
}
