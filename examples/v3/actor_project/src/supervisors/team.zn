// Team supervisor — manages Counter actors from the actors package

import zinc.Supervisor
import actors.Counter

class Team : Supervisor {
    init Counter c1
    init Counter c2

    init(Counter c1, Counter c2) {
        this.c1 = c1
        this.c2 = c2
    }
}
