// Sibling subpackage named `helpers` — used below as the cross-package
// context for the shadowing-regression test. Exported so the test code
// can detect that the subpackage really exists.

pub fn greet(String who): String {
    return "hello, ${who}"
}
