// Utility functions in the core package

pub fn formatUser(User u): String {
    return "${u.name} (age ${u.age})"
}

pub fn isAdult(User u): bool {
    return u.age >= 18
}

pub fn formatPrice(Product p): String {
    return "${p.name}: $${p.price}"
}
