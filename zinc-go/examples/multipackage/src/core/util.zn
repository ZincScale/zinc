// Utility functions in the core package

fn formatUser(User u): String {
    return "{u.name} (age {u.age})"
}

fn isAdult(User u): bool {
    return u.age >= 18
}

fn formatPrice(Product p): String {
    return "{p.name}: ${p.price}"
}
