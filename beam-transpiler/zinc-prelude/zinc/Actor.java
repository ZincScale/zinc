package zinc;

/** Marker: a class declared with `: Actor` becomes a gen_server + supervised process.
 *  Prelude stub — the transpiler owns all semantics; this exists so javac resolves the name. */
public interface Actor {}
