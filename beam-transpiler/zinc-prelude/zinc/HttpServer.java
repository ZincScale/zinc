package zinc;

/** HTTP server child: bind a port to a Router. A static child of the Application. Prelude stub. */
public final class HttpServer {
  public HttpServer(int port, Router routes) { throw Tag.stub(); }
}
