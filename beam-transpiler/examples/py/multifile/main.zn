from util import mathutil

def main() {
    s = mathutil.sum_to(4)         # module ref = lowercase file name (Pythonic)
    print(fmt.banner(f"{s}"))      # same-dir module, no import needed
    t = Tick()                     # a class is still CapWords
    t.inc()
    print(t.get())
}
