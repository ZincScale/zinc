# helper functions: annotated params, inferred return types, sibling calls
def add(a: int, b: int) {
    return a + b
}

def dbl(n: int) {
    return add(n, n)
}

def main() {
    print(add(3, 4))
    print(dbl(21))
}
