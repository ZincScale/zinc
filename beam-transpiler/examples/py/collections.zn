# Python list literals + lambda keyword; SAM lambda satisfies a 1-method interface
interface Op {
    def apply(n: int) -> int
}

def main() {
    xs = [10, 20, 30]
    print(xs[0] + xs[2])
    print(xs.length)
    f: Op = lambda n: n * 3
    print(f.apply(7))
}
