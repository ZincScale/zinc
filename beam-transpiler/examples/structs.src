record Point(int x, int y) {}

public class Main {
  public static void main(String[] args) {
    Point p = new Point(170, 4);
    p.x = 180;             // extension: mutation sugar over functional map update
    p.x += 5;
    System.out.println(p.x());  // 185
  }
}
