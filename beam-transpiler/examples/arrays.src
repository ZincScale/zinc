public class Main {
  public static void main(String[] args) {
    int[] xs = {10, 20, 30, 40};
    int acc = 0;
    for (int x : xs) {
      acc += x;
    }
    acc += xs.length - xs.length;     // exercise .length
    System.out.println(acc + xs[0] - xs[0]);  // 100, exercises indexing
  }
}
