void main() {
    var price = 9.99
    var pct = 0.2
    var discounted = price * (1.0 - pct)
    print("discount: ${discounted}")

    var remainder = 100 % (3 + 4)
    print("remainder: ${remainder}")

    var nested = ((10 - 2) * 3) + 1
    print("nested: ${nested}")
}
