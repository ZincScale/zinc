void main() {
    var scores = {"a": 1, "b": 2}
    scores["a"] = scores["a"] + 10
    print(scores["a"] + len(scores))

    var xs = [10, 20, 30]
    print(len(xs))
    print(len("hello"))

    var cfg = {"host": "localhost", "port": 8080}
    String host = cfg["host"]
    print(host)

    print(str(42) + "!")
}
