fn first_even_over(xs, threshold) {
  var result = 0
  for (x in xs) {
    if (x <= threshold) {
      continue
    }
    if (x % 2 == 0) {
      result = x
      break
    }
  }
  return result
}

fn main() {
  print(first_even_over([1, 3, 5, 8, 10], 4))
}
