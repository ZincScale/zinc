public class Main {
  public static void main(String[] args) {
    int n = 5;
    int total = 0;
    while (n > 0) {        // two mutated vars threaded through the loop
      total += n;
      n = n - 1;
    }
    System.out.println(total);  // 15
  }
}
