{plugins, [rebar_zinc]}.
{provider_hooks, [{pre, [{compile, {zinc, compile}}]},
                  {post, [{clean, {zinc, clean}}]}]}.
{src_dirs, [{"src", [{recursive, true}]}]}.
%% transpiler located via ZINC_HOME env (test.sh sets it); or:
%% {zinc, [{compiler_home, "/path/to/beam-transpiler"}]}.
